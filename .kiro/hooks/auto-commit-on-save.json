{
  "version": "v1",
  "hooks": [
    {
      "name": "Auto-commit al guardar archivos",
      "trigger": "PostFileSave",
      "matcher": "\\.(js|jsx|ts|tsx|css|json|sql|md|mjs)$",
      "action": {
        "type": "command",
        "command": "cd \"/Users/kennethho/taller-webapp Visual\" && git add -A && git diff --cached --quiet || git commit -m \"auto: cambios guardados $(date '+%Y-%m-%d %H:%M')\" && git push origin main",
        "timeout": 60
      }
    }
  ]
}